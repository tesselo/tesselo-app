import state from './predicted-layer.state'
import mutations from './predicted-layer.mutations'
import actions from './predicted-layer.actions'
import getters from './predicted-layer.getters'

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
