import state from './aggregation-area.state'
import mutations from './aggregation-area.mutations'
import actions from './aggregation-area.actions'
import getters from './aggregation-area.getters'

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
